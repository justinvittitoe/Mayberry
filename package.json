{
  "name": "mayberry-home-builder",
  "version": "1.0.0",
  "description": "A comprehensive home builder application for customizing and managing home plans",
  "main": "server/server.js",
  "scripts": {
    "start": "cd server && npm run start",
    "develop": "concurrently \"cd server && npm run watch\" \"wait-on tcp:4001 && cd client && npm run dev\"",
    "install": "cd server && npm i && cd ../client && npm i",
    "build": "concurrently \"cd server && npm run build\" \"cd client && npm run build\"",
    "render-build": "npm install && npm run build"
  },
  "keywords": [
    "home-builder",
    "real-estate",
    "customization",
    "graphql",
    "react"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.0",
    "wait-on": "^8.0.4"
  },
  "dependencies": {
    "@apollo/server": "^4.12.0",
    "graphql": "^16.11.0"
  }
}
